# Client

React + Vite frontend for the TAFL regex visualizer.

## Run

From the repo root:

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev` starts the local Vite dev server
- `npm run build` creates a production build
- `npm run preview` previews the production build locally
